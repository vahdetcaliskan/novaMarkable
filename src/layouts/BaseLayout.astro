---
import "../styles/global.css";
const { title = "novaMarkable" } = Astro.props;
---

<!doctype html>
<html lang="tr">
    <head>
        <meta charset="UTF-8" />
        <title>{title}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" type="image/x-icon" href="../../public/favicon.svg" />
    </head>

    <body class="bg-neutral-50 text-neutral-900">
        <header class="border-b border-neutral-200">
            <div class="max-w-6xl mx-auto px-6 py-4 font-semibold">
                novaMarkable
            </div>
        </header>

        <main class="max-w-6xl mx-auto px-6 py-16">
            <slot />
        </main>

        <footer class="border-t border-neutral-200 mt-24">
            <div class="max-w-6xl mx-auto px-6 py-8 text-sm text-neutral-500">
                © novaMarkable
            </div>
        </footer>
    </body>
</html>
